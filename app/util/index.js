'use strict'

const fs = require('fs')
const sentiment = require('sentiment')

// need a function to seperate meta from docs array
// then call write to JSON on each...

	//write meta to totals.json
	//write docs to year.json e.g. '1974.json'


function writeToJSON(arg) {
	//this should write a given array / object to file
	console.log('no errors so writing now!')
}



//ARBITRARY TRUMPLINE UPDATE

// SENTIMENT ANALYSIS

module.exports = {
	writeToJSON
}