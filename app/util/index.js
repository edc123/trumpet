'use strict'

const sentiment = require('sentiment')

function log(arg) {
	console.log(arg)
}

function writeToFile(arg) {
	console.log(arg)
}

// FILTER TRUMPLINES

//ARBITRARY TRUMPLINE UPDATE

// SENTIMENT ANALYSIS

module.exports = {
	log,
	writeToFile
}